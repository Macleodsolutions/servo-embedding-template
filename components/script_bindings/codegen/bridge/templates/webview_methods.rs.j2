// @generated by embedder_bridge.py - do not edit by hand.
macro_rules! embedder_bridge_webview_methods {
    {} => {
{% for iface in interfaces %}
{% for event in iface.events %}
        /// Fire `{{ event.attr_name }}` on `{{ iface.name }}` in this WebView's top-level document.
        pub fn {{ event.webview_fire_method }}(&self{% for f in event.fields %}, {{ f.name }}: {{ f.rust_type }}{% endfor %}) {
            self.inner()
                .servo
                .constellation_proxy()
                .send(servo_constellation_traits::EmbedderToConstellationMessage::{{ event.variant_name }}(
                    self.id(){% for f in event.fields %}, {{ f.name }}{% endfor %},
                ));
        }
{% endfor %}
{% endfor %}
    };
}
